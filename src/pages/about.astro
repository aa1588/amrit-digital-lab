---
import BaseLayout from '../layouts/BaseLayout.astro';
const base = import.meta.env.BASE_URL;
---

<BaseLayout title="About - Amrit Adhikari">
  <!-- Warm handwritten font for name -->
  <link href="https://fonts.googleapis.com/css2?family=Caveat:wght@500;600&display=swap" rel="stylesheet" />
  <div class="about-page">

    <!-- Floating shapes background -->
    <div class="floating-shapes">
      <div class="shape shape-1"></div>
      <div class="shape shape-2"></div>
      <div class="shape shape-3"></div>
      <div class="shape shape-4"></div>
    </div>

    <!-- ── Hello ───────────────────────────────────────────── -->
    <section class="hello">
      <div class="hero-grid">
        <div class="hero-text animate-slide-up">
          <p class="namaste">Namaste!</p>
          <h1>I'm <span class="name-highlight">Amrit</span></h1>
          <div class="taglines">
            <span>Software Engineer</span>
            <span>Coffee Driven Development</span>
            <span class="with-icon">
              <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="4" width="20" height="16" rx="2"/><path d="m22 7-8.97 5.7a1.94 1.94 0 0 1-2.06 0L2 7"/></svg>
              reachouttoamrit@gmail.com
            </span>
            <span class="with-icon">
              <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/><circle cx="12" cy="10" r="3"/></svg>
              Dallas, Texas
            </span>
          </div>
          <div class="hero-links">
            <a href="mailto:reachouttoamrit@gmail.com" class="hero-link" aria-label="Email">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="4" width="20" height="16" rx="2"/><path d="m22 7-8.97 5.7a1.94 1.94 0 0 1-2.06 0L2 7"/></svg>
            </a>
            <a href="https://www.linkedin.com/in/amrit-adhikari1/" target="_blank" class="hero-link" aria-label="LinkedIn">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/></svg>
            </a>
            <a href="https://github.com/aa1588" target="_blank" class="hero-link" aria-label="GitHub">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/></svg>
            </a>
            <a href="https://scholar.google.com/citations?user=zxIRPZYAAAAJ&hl=en" target="_blank" class="hero-link" aria-label="Google Scholar">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><path d="M12 24a7 7 0 1 1 0-14 7 7 0 0 1 0 14zm0-24L0 9.5l4.838 3.94A8 8 0 0 1 12 9a8 8 0 0 1 7.162 4.44L24 9.5z"/></svg>
            </a>
          </div>
        </div>
        <div class="hero-image animate-scale delay-1">
          <div class="avatar-frame">
            <img src={`${base}profile.png`} alt="Amrit Adhikari" class="avatar" />
          </div>
          <div class="avatar-status">
            <span class="status-dot"></span>
            <span>Open to chat</span>
          </div>
        </div>
      </div>
      <div class="scroll-indicator animate-fade delay-2">
        <span>Scroll to explore</span>
        <div class="scroll-arrow"></div>
      </div>
    </section>

    <!-- ── Quick Facts ─────────────────────────────────────── -->
    <section class="fun-facts">
      <!-- Nepal - Passport stamp style -->
      <div class="fact-item nepal" style="--delay: 0">
        <div class="passport-stamp">
          <div class="stamp-border">
            <span class="stamp-country">NEPAL</span>
            <span class="stamp-emoji">🇳🇵</span>
            <span class="stamp-text">ORIGIN</span>
            <span class="stamp-detail">Born & Raised</span>
          </div>
        </div>
      </div>

      <!-- Flight path -->
      <div class="fact-item flight" style="--delay: 1">
        <div class="boarding-pass">
          <div class="pass-left">
            <span class="pass-from">KTM</span>
            <span class="pass-label">From</span>
          </div>
          <div class="pass-middle">
            <span class="plane-icon">✈️</span>
            <div class="flight-path">
              <span class="dot"></span>
              <span class="line"></span>
              <span class="dot"></span>
            </div>
            <span class="pass-year">2016</span>
          </div>
          <div class="pass-right">
            <span class="pass-to">USA</span>
            <span class="pass-label">To</span>
          </div>
        </div>
      </div>

      <!-- Degrees - Achievement style -->
      <div class="fact-item degrees" style="--delay: 2">
        <div class="achievement">
          <div class="achievement-icon">🎓</div>
          <div class="achievement-badge">×2</div>
          <div class="achievement-text">
            <span class="achievement-title">Achievement Unlocked</span>
            <span class="achievement-desc">CS Degrees Collected</span>
          </div>
          <div class="achievement-sparkle">✨</div>
        </div>
      </div>

      <!-- Coffee - Mug style -->
      <div class="fact-item coffee" style="--delay: 3">
        <div class="coffee-mug">
          <div class="mug-steam">
            <span></span><span></span><span></span>
          </div>
          <div class="mug-body">
            <span class="mug-icon">☕</span>
          </div>
          <div class="mug-label">
            <span>RUNS ON</span>
            <strong>COFFEE</strong>
          </div>
        </div>
      </div>
    </section>

    <!-- ── My Story ────────────────────────────────────────── -->
    <section class="story reveal-section">
      <h2>The short version</h2>
      <div class="story-content">
        <p>
          I wrote my very first lines of code when I was 13, in QBASIC, in a tiny computer lab back in Nepal. The computers were old enough to belong in a museum, but they worked. And honestly, that was enough to spark something in me.
        </p>
        <p>
          In 2016, at 19, I moved to the US and started chasing this whole computer science path. It wasn't always easy, and I was figuring things out as I went, but looking back, I'm really grateful for how far it has taken me.
        </p>
        <p>
          These days, I'm especially interested in the space where language and machines meet — NLP, LLMs, and how we can make AI genuinely useful in the real world. <em>That's</em> the kind of work that keeps me curious.
        </p>
      </div>
    </section>

    <!-- ── Currently ───────────────────────────────────────── -->
    <section class="currently reveal-section">
      <h2>What I'm up to</h2>
      <div class="currently-grid">
        <div class="currently-card">
          <div class="card-number">01</div>
          <div class="card-content">
            <span class="card-title">Researching</span>
            <span class="card-value">NLP & Large Language Models</span>
            <p>Exploring what's possible with language AI</p>
          </div>
          <div class="card-glow"></div>
        </div>
        <div class="currently-card">
          <div class="card-number">02</div>
          <div class="card-content">
            <span class="card-title">Learning</span>
            <span class="card-value">System Design</span>
            <p>Always diving deeper into distributed systems</p>
          </div>
          <div class="card-glow"></div>
        </div>
        <div class="currently-card">
          <div class="card-number">03</div>
          <div class="card-content">
            <span class="card-title">Building</span>
            <span class="card-value">This blog</span>
            <p>Sharing what I learn along the way</p>
          </div>
          <div class="card-glow"></div>
        </div>
      </div>
    </section>

    <!-- ── Tech I Love ─────────────────────────────────────── -->
    <section class="tech-love reveal-section">
      <h2>My toolkit</h2>
      <div class="tech-grid">
        <div class="tech-item">
          <img src="https://cdn.simpleicons.org/openjdk/437291" alt="Java" />
          <span>Java</span>
        </div>
        <div class="tech-item">
          <img src="https://cdn.simpleicons.org/spring/6DB33F" alt="Spring" />
          <span>Spring</span>
        </div>
        <div class="tech-item">
          <img src="https://cdn.simpleicons.org/typescript/3178C6" alt="TypeScript" />
          <span>TypeScript</span>
        </div>
        <div class="tech-item">
          <img src="https://cdn.simpleicons.org/react/61DAFB" alt="React" />
          <span>React</span>
        </div>
        <div class="tech-item">
          <img src="https://cdn.simpleicons.org/postgresql/4169E1" alt="PostgreSQL" />
          <span>PostgreSQL</span>
        </div>
        <div class="tech-item">
          <img src="https://cdn.simpleicons.org/docker/2496ED" alt="Docker" />
          <span>Docker</span>
        </div>
        <div class="tech-item">
          <img src="https://cdn.simpleicons.org/kubernetes/326CE5" alt="Kubernetes" />
          <span>Kubernetes</span>
        </div>
        <div class="tech-item">
          <svg class="aws-icon" viewBox="0 0 24 24" fill="#FF9900" width="32" height="32"><path d="M6.763 10.036c0 .296.032.535.088.71.064.176.144.368.256.576.04.063.056.127.056.183 0 .08-.048.16-.152.24l-.503.335a.383.383 0 0 1-.208.072c-.08 0-.16-.04-.239-.112a2.47 2.47 0 0 1-.287-.375 6.18 6.18 0 0 1-.248-.471c-.622.734-1.405 1.101-2.347 1.101-.67 0-1.205-.191-1.596-.574-.391-.384-.59-.894-.59-1.533 0-.678.239-1.23.726-1.644.487-.415 1.133-.623 1.955-.623.272 0 .551.024.846.064.296.04.6.104.918.176v-.583c0-.607-.127-1.03-.375-1.277-.255-.248-.686-.367-1.3-.367-.28 0-.568.031-.863.103-.295.072-.583.16-.862.272a2.287 2.287 0 0 1-.28.104.488.488 0 0 1-.127.023c-.112 0-.168-.08-.168-.247v-.391c0-.128.016-.224.056-.28a.597.597 0 0 1 .224-.167c.279-.144.614-.264 1.005-.36a4.84 4.84 0 0 1 1.246-.151c.95 0 1.644.216 2.091.647.439.43.662 1.085.662 1.963v2.586zm-3.24 1.214c.263 0 .534-.048.822-.144.287-.096.543-.271.758-.51.128-.152.224-.32.272-.512.047-.191.08-.423.08-.694v-.335a6.66 6.66 0 0 0-.735-.136 6.02 6.02 0 0 0-.75-.048c-.535 0-.926.104-1.19.32-.263.215-.39.518-.39.917 0 .375.095.655.295.846.191.2.47.296.838.296zm6.41.862c-.144 0-.24-.024-.304-.08-.064-.048-.12-.16-.168-.311L7.586 5.55a1.398 1.398 0 0 1-.072-.32c0-.128.064-.2.191-.2h.783c.151 0 .255.025.31.08.065.048.113.16.16.312l1.342 5.284 1.245-5.284c.04-.16.088-.264.151-.312a.549.549 0 0 1 .32-.08h.638c.152 0 .256.025.32.08.063.048.12.16.151.312l1.261 5.348 1.381-5.348c.048-.16.104-.264.16-.312a.52.52 0 0 1 .311-.08h.743c.127 0 .2.065.2.2 0 .04-.009.08-.017.128a1.137 1.137 0 0 1-.056.2l-1.923 6.17c-.048.16-.104.263-.168.311a.51.51 0 0 1-.303.08h-.687c-.151 0-.255-.024-.32-.08-.063-.056-.119-.16-.15-.32l-1.238-5.148-1.23 5.14c-.04.16-.087.264-.15.32-.065.056-.177.08-.32.08zm10.256.215c-.415 0-.83-.048-1.229-.143-.399-.096-.71-.2-.918-.32-.128-.071-.215-.151-.247-.223a.563.563 0 0 1-.048-.224v-.407c0-.167.064-.247.183-.247.048 0 .096.008.144.024.048.016.12.048.2.08.271.12.566.215.878.279.319.064.63.096.95.096.502 0 .894-.088 1.165-.264a.86.86 0 0 0 .415-.758.777.777 0 0 0-.215-.559c-.144-.151-.416-.287-.807-.415l-1.157-.36c-.583-.183-1.014-.454-1.277-.813a1.902 1.902 0 0 1-.4-1.158c0-.335.073-.63.216-.886.144-.255.335-.479.575-.654.24-.184.51-.32.83-.415.32-.096.655-.136 1.006-.136.175 0 .359.008.535.032.183.024.35.056.518.088.16.04.312.08.455.127.144.048.256.096.336.144a.69.69 0 0 1 .24.2.43.43 0 0 1 .071.263v.375c0 .168-.064.256-.184.256a.83.83 0 0 1-.303-.096 3.652 3.652 0 0 0-1.532-.311c-.455 0-.815.071-1.062.223-.248.152-.375.383-.375.71 0 .224.08.416.24.567.159.152.454.304.877.44l1.134.358c.574.184.99.44 1.237.767.247.327.367.702.367 1.117 0 .343-.072.655-.207.926-.144.272-.336.511-.583.703-.248.2-.543.343-.886.447-.36.111-.734.167-1.142.167zM21.698 16.207c-2.626 1.94-6.442 2.969-9.722 2.969-4.598 0-8.74-1.7-11.87-4.526-.247-.223-.024-.527.27-.351 3.384 1.963 7.559 3.153 11.877 3.153 2.914 0 6.114-.607 9.06-1.852.439-.2.814.287.385.607zM22.792 14.961c-.336-.43-2.22-.207-3.074-.103-.255.032-.295-.192-.063-.36 1.5-1.053 3.967-.75 4.254-.399.287.36-.08 2.826-1.485 4.007-.215.184-.423.088-.327-.151.32-.79 1.03-2.57.695-2.994z"/></svg>
          <span>AWS</span>
        </div>
        <div class="tech-item">
          <img src="https://cdn.simpleicons.org/apachekafka/231F20" alt="Kafka" />
          <span>Kafka</span>
        </div>
        <div class="tech-item">
          <img src="https://cdn.simpleicons.org/redis/DC382D" alt="Redis" />
          <span>Redis</span>
        </div>
        <div class="tech-item">
          <img src="https://cdn.simpleicons.org/nodedotjs/339933" alt="Node.js" />
          <span>Node.js</span>
        </div>
        <div class="tech-item">
          <img src="https://cdn.simpleicons.org/python/3776AB" alt="Python" />
          <span>Python</span>
        </div>
      </div>
    </section>

    <!-- ── Footer Note ─────────────────────────────────────── -->
    <section class="footer-note reveal-section">
      <p>
        P.S. If you feel like you're just figuring it out as you go — <em>same here</em>. You're not alone in that. ✨
      </p>
    </section>

  </div>
</BaseLayout>

<script>
  // Intersection Observer for scroll animations
  const observerOptions = {
    threshold: 0.1,
    rootMargin: '0px 0px -50px 0px'
  };

  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('revealed');
      }
    });
  }, observerOptions);

  document.querySelectorAll('.reveal-section').forEach(el => {
    observer.observe(el);
  });

  document.querySelectorAll('.fact-card').forEach(el => {
    observer.observe(el);
  });
</script>

<style>
  .about-page {
    max-width: 700px;
    margin: 0 auto;
    padding: 0 2rem 4rem;
    position: relative;
  }

  /* ── Floating Shapes ───────────────────────────────────── */
  .floating-shapes {
    position: fixed;
    inset: 0;
    pointer-events: none;
    overflow: hidden;
    z-index: -1;
  }

  .shape {
    position: absolute;
    border-radius: 50%;
    opacity: 0.5;
    filter: blur(60px);
  }

  .shape-1 {
    width: 300px;
    height: 300px;
    background: var(--color-accent-light);
    top: 10%;
    right: -10%;
    animation: float1 20s ease-in-out infinite;
  }

  .shape-2 {
    width: 200px;
    height: 200px;
    background: #c8e6c9;
    bottom: 30%;
    left: -5%;
    animation: float2 25s ease-in-out infinite;
  }

  .shape-3 {
    width: 250px;
    height: 250px;
    background: #bbdefb;
    top: 50%;
    right: -15%;
    animation: float3 22s ease-in-out infinite;
  }

  .shape-4 {
    width: 180px;
    height: 180px;
    background: #ffe0b2;
    bottom: 10%;
    left: 10%;
    animation: float1 18s ease-in-out infinite reverse;
  }

  @keyframes float1 {
    0%, 100% { transform: translate(0, 0) scale(1); }
    33% { transform: translate(30px, -30px) scale(1.1); }
    66% { transform: translate(-20px, 20px) scale(0.9); }
  }

  @keyframes float2 {
    0%, 100% { transform: translate(0, 0) rotate(0deg); }
    50% { transform: translate(40px, -40px) rotate(10deg); }
  }

  @keyframes float3 {
    0%, 100% { transform: translate(0, 0); }
    25% { transform: translate(-30px, 30px); }
    75% { transform: translate(20px, -10px); }
  }

  /* ── Animations ────────────────────────────────────────── */
  @keyframes slideUp {
    from { opacity: 0; transform: translateY(40px); }
    to { opacity: 1; transform: translateY(0); }
  }

  @keyframes pop {
    0% { opacity: 0; transform: scale(0.5); }
    70% { transform: scale(1.1); }
    100% { opacity: 1; transform: scale(1); }
  }

  @keyframes scaleIn {
    from { opacity: 0; transform: scale(0.8); }
    to { opacity: 1; transform: scale(1); }
  }

  @keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
  }

  @keyframes bounce {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(8px); }
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; box-shadow: 0 0 0 0 rgba(34, 197, 94, 0.4); }
    50% { opacity: 0.8; box-shadow: 0 0 0 8px rgba(34, 197, 94, 0); }
  }

  @keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }

  @keyframes shimmer {
    0% { background-position: -200% 0; }
    100% { background-position: 200% 0; }
  }

  .animate-slide-up {
    animation: slideUp 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
  }

  .animate-pop {
    animation: pop 0.6s cubic-bezier(0.16, 1, 0.3, 1) forwards;
  }

  .animate-scale {
    animation: scaleIn 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
  }

  .animate-fade {
    animation: fadeIn 1s ease forwards;
  }

  .delay-1 { animation-delay: 0.1s; opacity: 0; animation-fill-mode: forwards; }
  .delay-2 { animation-delay: 0.2s; opacity: 0; animation-fill-mode: forwards; }
  .delay-3 { animation-delay: 0.4s; opacity: 0; animation-fill-mode: forwards; }

  /* ── Reveal on Scroll ──────────────────────────────────── */
  .reveal-section {
    opacity: 0;
    transform: translateY(30px);
    transition: all 0.8s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .reveal-section.revealed {
    opacity: 1;
    transform: translateY(0);
  }

  .fact-card {
    opacity: 0;
    transform: translateY(20px) rotate(-5deg);
    transition: all 0.5s cubic-bezier(0.16, 1, 0.3, 1);
    transition-delay: calc(var(--delay) * 0.1s);
  }

  .fact-card.revealed {
    opacity: 1;
    transform: translateY(0) rotate(0deg);
  }

  /* ── Hello Section ─────────────────────────────────────── */
  .hello {
    padding: 5rem 0 3rem;
    min-height: 90vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  .hero-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }

  .hero-text {
    text-align: left;
  }

  .hero-image {
    display: flex;
    flex-direction: column;
    align-items: center;
    position: relative;
    perspective: 1000px;
  }

  .namaste {
    font-family: 'Plus Jakarta Sans', sans-serif;
    font-size: 1.25rem;
    font-weight: 500;
    color: var(--color-accent);
    margin-bottom: 0.25rem;
    letter-spacing: 0.02em;
  }

  .taglines {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
    margin-bottom: 2rem;
  }

  .taglines span {
    font-family: 'Plus Jakarta Sans', sans-serif;
    font-size: 1rem;
    color: var(--color-text-secondary);
    position: relative;
    padding-left: 1.25rem;
  }

  .taglines span::before {
    content: '–';
    position: absolute;
    left: 0;
    color: var(--color-accent);
    font-weight: 500;
  }

  .taglines span.with-icon {
    display: flex;
    align-items: center;
    gap: 0.4rem;
    padding-left: 0;
  }

  .taglines span.with-icon::before {
    display: none;
  }

  .taglines span.with-icon svg {
    color: var(--color-accent);
    flex-shrink: 0;
  }

  .hero-links {
    display: flex;
    gap: 0.75rem;
    margin-top: 0.5rem;
  }

  .hero-link {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 44px;
    height: 44px;
    background: var(--color-bg);
    border: 2px solid var(--color-border);
    border-radius: 12px;
    color: var(--color-text-secondary);
    transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .hero-link:hover {
    border-color: var(--color-accent);
    color: var(--color-accent);
    transform: translateY(-4px);
    box-shadow: 0 8px 20px rgba(224, 122, 95, 0.2);
  }

  .hero-link svg {
    transition: transform 0.3s ease;
  }

  .hero-link:hover svg {
    transform: scale(1.1);
  }

  .hello h1 {
    font-family: 'Caveat', cursive;
    font-size: 2.75rem;
    font-weight: 600;
    color: var(--color-text);
    letter-spacing: 0.01em;
    margin-bottom: 1rem;
    line-height: 1.2;
  }

  .name-highlight {
    position: relative;
    display: inline-block;
    background: linear-gradient(135deg, var(--color-accent), #f5a623);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .name-highlight::after {
    content: '';
    position: absolute;
    bottom: 0.05em;
    left: -0.05em;
    right: -0.05em;
    height: 0.35em;
    background: linear-gradient(135deg, var(--color-accent-light), #fff3e0);
    z-index: -1;
    border-radius: 4px;
    transform: rotate(-2deg);
  }


  .avatar-frame {
    position: relative;
    padding: 8px;
    background: linear-gradient(135deg, var(--color-accent) 0%, #f5a623 100%);
    border-radius: 24px;
    transition: all 0.5s cubic-bezier(0.16, 1, 0.3, 1);
    transform: rotate(3deg) rotateY(-8deg) rotateX(2deg);
    transform-style: preserve-3d;
    box-shadow:
      8px 8px 0 rgba(224, 122, 95, 0.15),
      16px 16px 0 rgba(224, 122, 95, 0.08),
      0 25px 50px -12px rgba(0, 0, 0, 0.15);
  }

  .avatar-frame:hover {
    transform: rotate(0deg) rotateY(0deg) rotateX(0deg) scale(1.02);
    box-shadow:
      0 0 0 rgba(224, 122, 95, 0),
      0 0 0 rgba(224, 122, 95, 0),
      0 30px 60px -15px rgba(224, 122, 95, 0.35);
  }

  .avatar {
    width: 280px;
    height: 280px;
    border-radius: 20px;
    object-fit: cover;
    object-position: center top;
    display: block;
  }

  .frame-corner {
    position: absolute;
    width: 20px;
    height: 20px;
    border: 3px solid var(--color-accent);
    opacity: 0.6;
  }

  .corner-tl {
    top: -10px;
    left: -10px;
    border-right: none;
    border-bottom: none;
    border-radius: 8px 0 0 0;
  }

  .corner-tr {
    top: -10px;
    right: -10px;
    border-left: none;
    border-bottom: none;
    border-radius: 0 8px 0 0;
  }

  .corner-bl {
    bottom: -10px;
    left: -10px;
    border-right: none;
    border-top: none;
    border-radius: 0 0 0 8px;
  }

  .corner-br {
    bottom: -10px;
    right: -10px;
    border-left: none;
    border-top: none;
    border-radius: 0 0 8px 0;
  }

  .avatar-frame:hover .frame-corner {
    opacity: 1;
    animation: pulse-corner 1s ease infinite;
  }

  @keyframes pulse-corner {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.1); }
  }

  .avatar-status {
    position: absolute;
    bottom: -10px;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.5rem 1rem;
    background: var(--color-bg);
    border: 2px solid var(--color-border);
    border-radius: 100px;
    font-family: 'Plus Jakarta Sans', sans-serif;
    font-size: 0.8125rem;
    font-weight: 600;
    color: var(--color-text);
    white-space: nowrap;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
  }

  .status-dot {
    width: 8px;
    height: 8px;
    background: #22c55e;
    border-radius: 50%;
    animation: pulse 2s ease-in-out infinite;
  }

  .scroll-indicator {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    color: var(--color-text-muted);
    font-family: 'Plus Jakarta Sans', sans-serif;
    font-size: 0.8125rem;
    margin-top: 3rem;
  }

  .scroll-arrow {
    width: 20px;
    height: 20px;
    border-right: 2px solid var(--color-text-muted);
    border-bottom: 2px solid var(--color-text-muted);
    transform: rotate(45deg);
    animation: bounce 2s ease-in-out infinite;
  }

  /* ── Fun Facts ─────────────────────────────────────────── */
  .fun-facts {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1rem;
    padding: 2rem 0;
  }

  .fact-item {
    opacity: 0;
    transform: translateY(20px);
    animation: revealFact 0.6s cubic-bezier(0.16, 1, 0.3, 1) forwards;
    animation-delay: calc(var(--delay) * 0.15s);
  }

  @keyframes revealFact {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* ── Passport Stamp ─────────────────────────────────────── */
  .passport-stamp {
    background: #fef9f3;
    border: 3px solid #c41e3a;
    border-radius: 8px;
    padding: 0.75rem;
    transform: rotate(-4deg);
    transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
    cursor: pointer;
  }

  .passport-stamp:hover {
    transform: rotate(0deg) scale(1.08);
    box-shadow: 0 10px 30px rgba(196, 30, 58, 0.2);
  }

  .stamp-border {
    border: 2px dashed #c41e3a;
    border-radius: 4px;
    padding: 0.75rem 0.5rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.25rem;
  }

  .stamp-country {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.75rem;
    font-weight: 700;
    color: #c41e3a;
    letter-spacing: 0.15em;
  }

  .stamp-emoji {
    font-size: 1.5rem;
  }

  .stamp-text {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.5rem;
    font-weight: 600;
    color: #c41e3a;
    letter-spacing: 0.2em;
    opacity: 0.8;
  }

  .stamp-detail {
    font-family: 'Plus Jakarta Sans', sans-serif;
    font-size: 0.625rem;
    color: #666;
    margin-top: 0.25rem;
  }

  /* ── Boarding Pass ──────────────────────────────────────── */
  .boarding-pass {
    background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
    border-radius: 12px;
    padding: 1rem;
    display: flex;
    align-items: center;
    justify-content: space-between;
    position: relative;
    overflow: hidden;
    cursor: pointer;
    transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .boarding-pass::before {
    content: '';
    position: absolute;
    left: -8px;
    top: 50%;
    transform: translateY(-50%);
    width: 16px;
    height: 16px;
    background: var(--color-bg);
    border-radius: 50%;
  }

  .boarding-pass::after {
    content: '';
    position: absolute;
    right: -8px;
    top: 50%;
    transform: translateY(-50%);
    width: 16px;
    height: 16px;
    background: var(--color-bg);
    border-radius: 50%;
  }

  .boarding-pass:hover {
    transform: scale(1.05);
    box-shadow: 0 15px 40px rgba(26, 26, 46, 0.4);
  }

  .pass-left, .pass-right {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.125rem;
  }

  .pass-from, .pass-to {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.875rem;
    font-weight: 700;
    color: #fff;
  }

  .pass-label {
    font-family: 'Plus Jakarta Sans', sans-serif;
    font-size: 0.5rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: rgba(255, 255, 255, 0.5);
  }

  .pass-middle {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.25rem;
  }

  .plane-icon {
    font-size: 1rem;
    animation: flyPlane 2s ease-in-out infinite;
  }

  @keyframes flyPlane {
    0%, 100% { transform: translateX(0); }
    50% { transform: translateX(3px); }
  }

  .flight-path {
    display: flex;
    align-items: center;
    gap: 0;
  }

  .flight-path .dot {
    width: 4px;
    height: 4px;
    background: var(--color-accent);
    border-radius: 50%;
  }

  .flight-path .line {
    width: 24px;
    height: 1px;
    background: linear-gradient(90deg, var(--color-accent), rgba(255,255,255,0.3), var(--color-accent));
  }

  .pass-year {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.625rem;
    color: var(--color-accent);
    font-weight: 600;
  }

  /* ── Achievement Badge ──────────────────────────────────── */
  .achievement {
    background: linear-gradient(135deg, #2d1f3d 0%, #1a1a2e 100%);
    border: 2px solid #6c5ce7;
    border-radius: 12px;
    padding: 1rem 0.75rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.375rem;
    position: relative;
    cursor: pointer;
    transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
    overflow: hidden;
  }

  .achievement::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, rgba(108, 92, 231, 0.2) 0%, transparent 50%);
    pointer-events: none;
  }

  .achievement:hover {
    transform: scale(1.05);
    border-color: #a29bfe;
    box-shadow: 0 0 30px rgba(108, 92, 231, 0.4);
  }

  .achievement-icon {
    font-size: 1.5rem;
    position: relative;
    z-index: 1;
  }

  .achievement-badge {
    position: absolute;
    top: 8px;
    right: 8px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.625rem;
    font-weight: 700;
    color: #ffd700;
    background: rgba(255, 215, 0, 0.2);
    padding: 0.125rem 0.375rem;
    border-radius: 4px;
  }

  .achievement-text {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.125rem;
    position: relative;
    z-index: 1;
  }

  .achievement-title {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.5rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: #a29bfe;
  }

  .achievement-desc {
    font-family: 'Plus Jakarta Sans', sans-serif;
    font-size: 0.6875rem;
    font-weight: 600;
    color: #fff;
  }

  .achievement-sparkle {
    position: absolute;
    top: 6px;
    left: 6px;
    font-size: 0.75rem;
    animation: sparkle 1.5s ease-in-out infinite;
  }

  @keyframes sparkle {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.5; transform: scale(0.8); }
  }

  /* ── Coffee Mug ─────────────────────────────────────────── */
  .coffee-mug {
    background: var(--color-bg);
    border: 2px solid var(--color-border);
    border-radius: 12px;
    padding: 1rem 0.75rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.25rem;
    position: relative;
    cursor: pointer;
    transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .coffee-mug:hover {
    transform: scale(1.05);
    border-color: #8B4513;
    box-shadow: 0 10px 30px rgba(139, 69, 19, 0.15);
  }

  .mug-steam {
    display: flex;
    gap: 4px;
    height: 16px;
  }

  .mug-steam span {
    width: 3px;
    background: linear-gradient(to top, rgba(139, 69, 19, 0.3), transparent);
    border-radius: 50%;
    animation: steam 2s ease-in-out infinite;
  }

  .mug-steam span:nth-child(1) {
    height: 10px;
    animation-delay: 0s;
  }

  .mug-steam span:nth-child(2) {
    height: 14px;
    animation-delay: 0.3s;
  }

  .mug-steam span:nth-child(3) {
    height: 10px;
    animation-delay: 0.6s;
  }

  @keyframes steam {
    0%, 100% {
      transform: translateY(0) scaleY(1);
      opacity: 0.6;
    }
    50% {
      transform: translateY(-4px) scaleY(1.2);
      opacity: 0.3;
    }
  }

  .coffee-mug:hover .mug-steam span {
    animation-duration: 1s;
  }

  .mug-body {
    font-size: 1.75rem;
  }

  .mug-label {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0;
    text-align: center;
  }

  .mug-label span {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.5rem;
    letter-spacing: 0.15em;
    color: var(--color-text-muted);
  }

  .mug-label strong {
    font-family: 'Plus Jakarta Sans', sans-serif;
    font-size: 0.75rem;
    font-weight: 700;
    color: #8B4513;
  }

  /* ── Section Styles ────────────────────────────────────── */
  section {
    padding: 3rem 0;
  }

  section h2 {
    font-family: 'Plus Jakarta Sans', sans-serif;
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--color-text);
    margin-bottom: 1.5rem;
  }


  /* ── Story Section ─────────────────────────────────────── */
  .story-content {
    font-size: 1.125rem;
    line-height: 1.85;
    color: var(--color-text-secondary);
  }

  .story-content p {
    margin-bottom: 1.5rem;
  }

  .story-content p:last-child {
    margin-bottom: 0;
  }

  .story-content em {
    color: var(--color-text);
    font-style: normal;
    font-weight: 600;
  }

  .highlight-text {
    background: linear-gradient(135deg, var(--color-accent-light) 0%, #fff3e0 100%);
    padding: 0.1em 0.3em;
    border-radius: 4px;
  }

  /* ── Currently Section ─────────────────────────────────── */
  .currently h2 {
    text-align: center;
    justify-content: center;
    font-size: 1.75rem;
    margin-bottom: 2.5rem;
  }

  .currently-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.25rem;
  }

  .currently-card {
    position: relative;
    padding: 2rem 1.5rem;
    background: var(--color-bg);
    border: 2px solid var(--color-border);
    border-radius: 20px;
    overflow: hidden;
    transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .currently-card:hover {
    border-color: var(--color-accent);
    transform: translateY(-8px);
  }

  .currently-card:hover .card-glow {
    opacity: 1;
  }

  .currently-card:hover .card-number {
    color: var(--color-accent);
    transform: scale(1.1);
  }

  .card-glow {
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, var(--color-accent-light) 0%, transparent 60%);
    opacity: 0;
    transition: opacity 0.4s ease;
    pointer-events: none;
  }

  .card-number {
    font-family: 'JetBrains Mono', monospace;
    font-size: 3rem;
    font-weight: 700;
    color: var(--color-border);
    line-height: 1;
    margin-bottom: 1.5rem;
    transition: all 0.3s ease;
  }

  .card-content {
    position: relative;
    z-index: 1;
  }

  .card-title {
    display: block;
    font-family: 'Plus Jakarta Sans', sans-serif;
    font-size: 0.6875rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    color: var(--color-accent);
    margin-bottom: 0.5rem;
  }

  .card-value {
    display: block;
    font-family: 'Plus Jakarta Sans', sans-serif;
    font-size: 1.25rem;
    font-weight: 700;
    color: var(--color-text);
    margin-bottom: 0.5rem;
    line-height: 1.3;
  }

  .card-content p {
    font-size: 0.9375rem;
    color: var(--color-text-secondary);
    line-height: 1.5;
    margin: 0;
  }

  @media (max-width: 768px) {
    .currently-grid {
      grid-template-columns: 1fr;
    }
  }

  /* ── Tech Section ──────────────────────────────────────── */
  .tech-love h2 {
    text-align: center;
    justify-content: center;
  }

  .tech-grid {
    display: grid;
    grid-template-columns: repeat(6, 1fr);
    gap: 1rem;
  }

  .tech-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.75rem;
    padding: 1.25rem 1rem;
    background: var(--color-bg);
    border: 2px solid var(--color-border);
    border-radius: 16px;
    cursor: pointer;
    transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .tech-item:hover {
    border-color: var(--color-accent);
    transform: translateY(-6px);
    box-shadow: 0 12px 30px rgba(0, 0, 0, 0.1);
  }

  .tech-item img,
  .tech-item svg {
    width: 32px;
    height: 32px;
    object-fit: contain;
    transition: transform 0.3s ease;
  }

  .tech-item:hover img,
  .tech-item:hover svg {
    transform: scale(1.15);
  }

  .tech-item span {
    font-family: 'Plus Jakarta Sans', sans-serif;
    font-size: 0.8125rem;
    font-weight: 600;
    color: var(--color-text);
    text-align: center;
  }

  @media (max-width: 768px) {
    .tech-grid {
      grid-template-columns: repeat(4, 1fr);
    }
  }

  @media (max-width: 480px) {
    .tech-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }


  /* ── Footer Note ───────────────────────────────────────── */
  .footer-note {
    text-align: center;
    padding: 3rem 0;
  }

  .footer-note p {
    font-size: 1.125rem;
    color: var(--color-text-secondary);
    line-height: 1.7;
  }

  .footer-note em {
    color: var(--color-accent);
    font-style: normal;
    font-weight: 600;
  }

  /* ── Responsive ────────────────────────────────────────── */
  @media (max-width: 768px) {
    .hero-grid {
      grid-template-columns: 1fr;
      gap: 2.5rem;
    }

    .hero-text {
      text-align: center;
      order: 2;
    }

    .hero-image {
      order: 1;
    }

    .taglines {
      align-items: center;
    }

    .taglines span {
      padding-left: 1.25rem;
    }

    .taglines span.with-icon {
      padding-left: 0;
    }

    .hero-links {
      justify-content: center;
    }

    .avatar {
      width: 200px;
      height: 200px;
    }
  }

  @media (max-width: 640px) {
    .about-page {
      padding: 0 1.5rem 3rem;
    }

    .hello {
      min-height: auto;
      padding: 3rem 0;
    }

    .hello h1 {
      font-size: 2.25rem;
    }

    .namaste {
      font-size: 1rem;
    }

    .fun-facts {
      grid-template-columns: repeat(2, 1fr);
      gap: 0.75rem;
    }

    .boarding-pass {
      padding: 0.75rem 0.5rem;
    }

    .pass-from, .pass-to {
      font-size: 0.75rem;
    }

    .flight-path .line {
      width: 16px;
    }

    .floating-shapes {
      display: none;
    }

    .avatar {
      width: 180px;
      height: 180px;
    }
  }
</style>
